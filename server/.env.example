# Works without an API key.
AI_PROVIDER=fake

# Comma-separated frontend origins permitted to call this API.
CORS_ORIGINS=https://pfalli.github.io,http://localhost:8000,http://localhost:8080

# For AI_PROVIDER=openai only. Never commit a real key.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
